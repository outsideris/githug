'use strict';

describe('Controllers >', function() {

  describe('Install Controller', function() {
    it('standalone 모드가 아니면 인스톨 페이지로 이동한다', function() {
      // TODO: testing depend on standalone mode
      expect(1).toBe(1);
    });
  });

});
